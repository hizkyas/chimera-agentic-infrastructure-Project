[project]
name = "project-chimera"
version = "0.1.0"
dependencies = [
    "pydantic>=2.0",  # For Executable Specs
    "pytest>=7.0",    # For TDD
    "sqlalchemy",     # For DB Schema
    "httpx",          # For Agent Skills
    "pyyaml>=6.0.3",
]
